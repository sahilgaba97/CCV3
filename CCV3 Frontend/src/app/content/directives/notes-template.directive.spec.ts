import { NotesTemplateDirective } from './notes-template.directive';

describe('NotesTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new NotesTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
