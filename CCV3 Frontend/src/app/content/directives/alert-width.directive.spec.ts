import { AlertWidthDirective } from './alert-width.directive';

describe('AlertWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertWidthDirective();
    expect(directive).toBeTruthy();
  });
});
