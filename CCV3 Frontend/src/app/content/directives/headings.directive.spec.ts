import { HeadingsDirective } from './headings.directive';

describe('HeadingsDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingsDirective();
    expect(directive).toBeTruthy();
  });
});
